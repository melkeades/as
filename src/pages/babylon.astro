---
import BaseLayout from '../layouts/BaseLayout.astro'
import { Debug } from 'astro:components'

import Babylon from '../components/babylon/app'
---

<!-- <Debug log={allJsx.length} /> -->
<BaseLayout anotherProp="">
  <Babylon client:load is:raw />
</BaseLayout>

<style is:global>
  .htmlScreen iframe {
    width: 1024px;
    height: 670px;
  }
  .qwe {
    /* opacity: 0.5 !important; */
    position: fixed !important;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
  }
  .app {
    position: absolute;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
  }
  body {
    /* background: #333; */
    /* height: 100vh; */
  }
  canvas {
    height: 80vh;
  }
  #app {
    background-color: #333;
  }
  .canvasHtml1 > div {
    font-family: sans-serif;
    padding: 1em;
    background: #00000077;
    color: white;
    /* position: absolute; */
    border-radius: 1em;
    user-select: none;
    white-space: nowrap;
  }
</style>
